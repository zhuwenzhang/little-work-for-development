1.注册界面（只能注册普通用户）

1.注册界面（需要用户输入三个东西，用户id，password，用户姓名）（注册的时间需要检验一下用户id在数据库中是否有冲突）
有冲突弹提示框让他重新注册。注册成功后，下面弹提示注册成功，2秒钟自动跳转到登录界面

2.登录界面（用户输入用户id，password），用户id数据库没有就弹提示框让他注册，2秒钟自动跳转到注册界面

识别一下用户id对应的用户类型（1表示普通用户）跳传到普通用户浏览界面
0表示管理员和2表示超级用户则跳转到管路员界面


3.普通用户浏览界面
（
   这是一个前台界面，需要做到。
文章列表 / 文章详情（只展示已发布内容）
查询（同一入口或同一视图，按模式分支）：
按文章题目（模糊匹配标题）
按发表时间（日期/日期区间）
按栏目（栏目 ID 或名称）
）

4.管路员界面（如果用户类型是2表示超级用户，需要新增加一栏是用户管理，可以增加和停用普通用户和管路员用户）
（如果用户类型是0表示管理员，在同一栏也是用户管理，可以增加和停用普通用户）
（
需要做到
管理端接口/视图
栏目：增、删、改、查（列表 + 详情/编辑）
文章：增、删、改、查；创建/编辑时选择栏目
）
拓展1
1.用户在前台文章浏览界面(就是文章内容下面)，新增加一个（发表评论）按钮,同时这个界面
下面也可以看到以发表的评论，这里能看到的是数据库评论表（comment）表里面是否显示是0的，也就是可以显示的，是否显示字段为1的时候不显示。
（点击发表评论即可把自己的评论发表了）


2.管理员界面左侧需要新增加一个评论管理模块，右侧是评论管理，
可以通过评论类容或者发表人的姓名查询评论
显示的列有：评论内容，姓名（就是评论的人），评论时间，是否可见（0 是显示，1 是删除状态），文章标题（就显示前五个字）
管理员可以对评论进行选择显示或者不显示。
